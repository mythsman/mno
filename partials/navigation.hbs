<nav class="cover-navigation cover-navigation--primary">
  <ul class="navigation">
    {{#foreach navigation}}
      <li class="navigation__item"><a href="{{url}}" title="{{label}}" class="blog-button">{{label}}</a></li>
    {{/foreach}}
  </ul>
</nav>
